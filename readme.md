# Jobly Backend

This is the Express backend for Jobly, version 2.

To run this:

    node server.js
    
To run the tests:

    jest -i
# jobly
---

## README (`jobly`)

```md
# Jobly Backend (API)

Express + PostgreSQL API for Jobly — authentication, companies, jobs, and applications.

## Features
- JWT auth (register/login)
- Companies list + company detail + search
- Jobs list + search
- Applications: apply to job + list user applications (and optional withdraw)

## Tech Stack
- Node.js
- Express
- PostgreSQL
- JSON Schema validation (if included in your project)
- JWT auth
- bcrypt password hashing

## Getting Started

### Prerequisites
- Node.js (LTS recommended)
- PostgreSQL running locally

### Install
```bash
npm install
